import { ChangeDetectionStrategy,Component,inject,OnInit,signal } from '@angular/core';import { LucideBell,LucideCheckCheck,LucideRefreshCw } from '@lucide/angular';import { ApiClientService } from '../../../../core/http/api-client.service';import { normalizePaged,PagedResult } from '../../../../shared/models/paged-result.model';interface N{id:string;title?:string;message?:string;body?:string;isRead?:boolean;createdAtUtc?:string;type?:string}@Component({selector:'app-notifications-page',standalone:true,imports:[LucideBell,LucideCheckCheck,LucideRefreshCw],templateUrl:'./notifications.page.html',styleUrl:'./notifications.page.css',changeDetection:ChangeDetectionStrategy.OnPush})export class NotificationsPage implements OnInit{private api=inject(ApiClientService);rows=signal<N[]>([]);ngOnInit(){this.load()}load(){this.api.get<PagedResult<N>|N[]>('notifications',{pageNumber:1,pageSize:100}).subscribe(v=>this.rows.set(normalizePaged(v).items))}read(n:N){if(n.isRead)return;this.api.patch(`notifications/${n.id}/read`,{}).subscribe(()=>this.load())}}
